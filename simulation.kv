#:kivy 2.0
<CircularButton>:
    canvas:
        Color: 
            rgb: (self.color)
        RoundedRectangle:
            size: (self.size)
            pos: (self.pos)
            radius: [20, ]
<Menu>:
    size: (self.size)