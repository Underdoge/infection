#:kivy 2.0
<CircularButton>:
    canvas:
        Color: 
            rgb: (self.color)
        RoundedRectangle:
            size: (self.size)
            pos: (self.pos)
            radius: [self.size[0], ]
<Menu>:
    size: (self.size)